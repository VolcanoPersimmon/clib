#是注释
#每个有效配置项用 "=" 处理, "="前不超过40个字符， 等号后不超过400个字符；

[Socket]
ListenPort = 5678

DBInfo = 127.0.0.1;1234;myr;123456;mydb_g

#日志相关
[Log]
#日志文件输出目录和文件名
#Log=logs/error.log
Log=error.log

#只打印日志等级<= 数字 的日志到日志文件中 ，日志等级0-8,0级别最高，8级别最低。
LogLevel = 8

#进程相关
[Proc]
#创建worker进程个数
WorkerProcesses = 8